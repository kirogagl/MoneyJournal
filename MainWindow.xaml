<Window x:Class="finWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:col="clr-namespace:System.Collections;assembly=mscorlib" 
        xmlns:local="clr-namespace:finWpf"
        mc:Ignorable="d"
        
        Title="Cash Log" Height="450" Width="800">
    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>

            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="0.26*"></ColumnDefinition>
            <ColumnDefinition Width="0.20*"></ColumnDefinition>

            <ColumnDefinition Width="0.4*"></ColumnDefinition>

        </Grid.ColumnDefinitions>

        <DataGrid x:Name="dataGrid" AutoGenerateColumns="False" Grid.RowSpan="3">
            <DataGrid.Resources>
                <col:ArrayList x:Key="MoneyRecords">

                </col:ArrayList>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Money" Binding="{Binding [0]}" Width="Auto" CanUserSort="True" IsReadOnly="True"/>
                <DataGridTextColumn Header="Money Unit" Binding="{Binding [1]}" Width="Auto" IsReadOnly="True"/>
                <DataGridTextColumn Header="Descriprion" Binding="{Binding [2]}" Width="Auto" IsReadOnly="True"/>
                <DataGridTextColumn Header="Date" Binding="{Binding [3]}" Width="Auto" CanUserSort="True" IsReadOnly="True"/>
                <DataGridTextColumn Header="Type" Binding="{Binding [4]}" Width="Auto" CanUserSort="True" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <GridSplitter Grid.Column="1" Grid.Row="0" HorizontalAlignment="Center" Width="3" Grid.RowSpan="3"/>

        <Label Content="Current account = " Grid.Column="2" Height="25" VerticalAlignment="Top" Margin="0,10,0,0"/>
        <TextBlock x:Name="acc" Grid.Column="3" TextWrapping="Wrap" VerticalAlignment="Top" Height="20" Width="Auto" Margin="0,16,0,0" />
        <ComboBox x:Name="unitFlag" SelectedIndex="0" SelectionChanged="unitFlag_SelectionChanged" Grid.Column="4" Grid.Row="0" Width="Auto" Height="30" VerticalAlignment="Top" Margin="0,10,0,0">
            <TextBlock Text="United States dollar"/>
            <TextBlock Text="Russian ruble"/>
            <TextBlock Text="Euro"/>
            <TextBlock Text="Swiss franc"/>
            <TextBlock Text="Pound sterling"/>
            <TextBlock Text="Japanese yen"/>
            <TextBlock Text="Ukrainian hryvnia"/>
            <TextBlock Text="Kazakhstani tenge"/>
            <TextBlock Text="Belarusian ruble"/>
            <TextBlock Text="Turkish lira"/>
            <TextBlock Text="Chinese Yuan Renminbi"/>
        </ComboBox>
        <Button x:Name="editMoney" Width="170" Height="30" Background="LightBlue" Grid.Column="4" Grid.Row="1" Content="Edit Money Record" Click="editMoney_Click"/>
        <Button x:Name="addMoney" Width="170" Height="30" Background="LightGreen" Grid.Column="2" Grid.ColumnSpan="2" Grid.Row="2"  Content="Add Money Record" Click="addMoney_Click" HorizontalAlignment="Right" VerticalAlignment="Bottom"/>
        <Button x:Name="removeMoney" Width="170" Height="30" Background="LightPink" Grid.Column="4" Grid.Row="2" Content="Remove Money Record" Click="removeMoney_Click" VerticalAlignment="Bottom"/>
    </Grid>
</Window>
